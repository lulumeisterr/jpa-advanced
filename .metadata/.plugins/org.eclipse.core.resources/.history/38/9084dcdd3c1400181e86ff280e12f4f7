package br.com.fiap.teste;

public class Comandos {
	

/*
 *  persist() = insert

	find() = select

	merge() = update

	remove() = delete
	
 */
	
	
	/* Estados 
	 * 
	 * [ Transient ] - A aplicação sempre começa no estado transaction = Quando você da new em um objeto e preenche as informações
	 *  e precisamos transformar esse metodo em maneger , chamando o metodo persiste , para
	 *  realizar a sincronização automatica com o banco , e isso dura ate vc chamar o entityManager.close();
	 *  
	 *  Quando você chama o entityManager.close as entidades que era maneger passa a ser detacher , Elas continuam
	 *  com a representação no banco mas não são mais gerenciadas.
	 *  
	 *  E Para transformar essa entidade que virou detacher em manager novamente , deveremos usar o metodo
	 *  marger() , ele faz um update na entidade para passar ser manager novamente e ter sincronização.
	 *  
	 *  E por ultimo tem o estado remove , quando não há mais representações no banco mas a entidade continua
	 *  
	 */

}
